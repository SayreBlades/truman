[project]
name = "temperature-converter"
version = "0.1.0"
description = "Example project demonstrating truman devcontainer usage"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.3.3",
]
